# Runtime state of an installation: isolated homes, credentials, baselines, logs. Never commit.
state/
config.json
# Pinned Node dependencies are installed by install.sh (npm ci).
runtime/node_modules/
# Build artifact of ZcodeSafe.swift; install.sh rebuilds it.
native/ZcodeSafeLauncher
__pycache__/
*.pyc
.DS_Store
# Operator logs kept only in the installed copy.
HANDOFF.md
REPAIRS.md
RECOVERY.md
COMPATIBILITY.md
ZCODE-REVIEW.md
.codex/
review-zcode/
archive/
